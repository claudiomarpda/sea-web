spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/db_example
    username: root
    password: 123


logging:
  level:
    root: info
    org.springframework: info
    org.springframework.boot: info